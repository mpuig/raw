[project]
name = "raw-transports"
version = "0.1.0"
description = "RAW Platform Transports - container for transport packages"
readme = "README.md"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
