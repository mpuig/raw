[project]
name = "raw-integrations"
version = "0.1.0"
description = "RAW Platform Integrations - container for integration packages"
readme = "README.md"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
