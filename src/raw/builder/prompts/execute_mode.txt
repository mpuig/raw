EXECUTE MODE ACTIVE - FULL ACCESS

You are now in EXECUTE mode. You have full access to write files, run commands,
and modify the codebase to implement the approved plan.

APPROVED PLAN:
{plan}

EXPECTED QUALITY GATES:
{gates}

YOUR TASK:

Follow the approved plan step-by-step. Make the necessary changes to implement
each step. Focus on making the workflow pass the quality gates listed above.

GUIDELINES:

1. **Follow the plan**: Implement each numbered step in order
2. **Be precise**: Make minimal, focused changes (no over-engineering)
3. **Verify imports**: Ensure all tool imports exist in tools/
4. **Update dry_run.py**: Add/update mocks for new functionality
5. **Test coverage**: Update test.py if adding new behavior

AVAILABLE TOOLS:

- Write: Create new files
- Edit: Modify existing files (use exact string matching)
- Read: Read files before editing
- Bash: Run shell commands
- raw validate: Validate workflow structure

After you complete the implementation, the builder will automatically run the
quality gates ({gate_list}). If gates fail, you'll receive feedback and enter
PLAN mode again to revise the approach.

BEGIN IMPLEMENTATION:
