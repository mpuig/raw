# {{ name }}

**ID:** `{{ workflow_id }}`
**Status:** draft

## Intent

{{ intent }}

## Usage

```bash
# 1. Implement run.py in this directory

# 2. Test with mock data
raw run {{ workflow_id }} --dry

# 3. Run the workflow
raw run {{ workflow_id }}
```
