# RAW Context

> Run `raw show --context` after compaction or new session

## Quick Reference

```bash
raw list tools -s "capability"     # Search tools (DO THIS FIRST)
raw list tools                     # Browse tools
raw create <name> --intent "..."   # Create workflow
raw create <name> --tool -d "..."  # Create tool (only if search finds nothing)
raw run <id> --dry                 # Test with mocks
raw run <id> [args]                # Execute
```

## Key Rules

1. **SEARCH FIRST** - `raw list tools -s` before creating tools
2. **Tools in `tools/`** - Reusable packages, auto-snapshotted on run
3. **Test before delivery** - `raw run --dry` before telling user it's ready
4. **Duplicate to modify** - use `raw create --from` to modify existing workflows

## Creating Workflows

```bash
raw create stock-analysis --intent "Fetch TSLA data, calculate RSI"
# Implement run.py using tools from tools/
raw run <id> --dry                 # Test
raw run <id> --ticker TSLA         # Execute
```

## Creating Tools

```bash
raw list tools -s "stock data"     # Check existing first!
raw create fetch_stock --tool -d "Fetch stock data from yfinance"
# Implement tools/fetch_stock/tool.py + __init__.py + config.yaml
```

---

{% if ctx.has_workflows %}
## Workflows ({{ ctx.workflow_count }})

{% for wf in ctx.workflows %}
- {{ wf.status_icon }} **{{ wf.name }}** - {{ wf.truncated_intent }}
{% endfor %}
{% endif %}

{% if ctx.has_tools %}
## Tools ({{ ctx.tool_count }})

{% for tool in ctx.tools %}
- **{{ tool.name }}**: {{ tool.truncated_description }}
{% endfor %}
{% endif %}
